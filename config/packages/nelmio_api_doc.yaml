nelmio_api_doc:
    documentation:
        info:
            title: API Hooly Foodtrucks
            description: API pour la gestion des réservations de foodtrucks sur les campus de Hooly
            version: 1.0.0
        components:
            securitySchemes:
                Bearer:
                    type: http
                    scheme: bearer
                    bearerFormat: JWT
    areas:
        path_patterns:
            - ^/api(?!/doc$)